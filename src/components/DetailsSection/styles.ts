import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  main: { flex: 1, gap: 4 },
  title: {
    textTransform: 'capitalize',
    fontSize: 18,
    fontWeight: '500',
  },
})
